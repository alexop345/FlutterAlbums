import 'package:flutter/material.dart';

class AppDimens {
  static const double normalSpacing = 8;
  static const double defaultSpacing = 16;


  static const EdgeInsets containerNormalSpacingAll = EdgeInsets.all(normalSpacing);
  static const EdgeInsets containerDefaultSpacingAll = EdgeInsets.all(defaultSpacing);


  static const EdgeInsets leftNormalSpacing = EdgeInsets.only(left: normalSpacing);

  static const EdgeInsets bottomDefaultSpacing = EdgeInsets.only(bottom: defaultSpacing);
  static const EdgeInsets topDefaultSpacing = EdgeInsets.only(top: defaultSpacing);
}
